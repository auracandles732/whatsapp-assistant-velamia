services:
  - type: web
    name: whatsapp-assistant-velamia
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
